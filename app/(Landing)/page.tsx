export default function Landing({}) {
    return (
        <div
            className={
                'flex h-full w-full flex-col items-center justify-center'
            }
        >
            <h1 className={'text-3xl font-bold text-primary'}>Kusodu</h1>
            <p className={'text-lg text-primary'}>The modern way to sudoku !</p>
        </div>
    );
}
